import styled from 'styled-components'
import theme from 'styles/theme'


export const ReadStyled = styled.p`

`

export const ContainerStyled = styled.div`
  max-width: 1220px;
  margin: 0 auto;
  padding: 0 10px;
`

export const color = {
  textColor: theme.colors.slate,
  bgColor: theme.colors.dark,
  primary: theme.colors.blue[3]
}

export const themeName = 'Liar'